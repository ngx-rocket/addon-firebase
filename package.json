{
  "name": "generator-addon-firebase",
  "version": "1.0.0",
  "description": "ngX-Rocket Firebase deploy add-on",
  "main": "./generators/app/index.js",
  "scripts": {
    "test": "eslint generators/app/*.js",
    "postpublish": "git tag -a $npm_package_version -m '$npm_package_version' && git push --tags"
  },
  "keywords": [
    "yeoman-generator",
    "ngx-rocket-addon",
    "firebase",
    "deploy"
  ],
  "author": "Yohan Lasorsa",
  "license": "MIT",
  "dependencies": {
    "@ngx-rocket/core": "^2.1.0",
    "chalk": "^2.0.1"
  },
  "devDependencies": {
    "eslint": "^4.3.0",
    "eslint-config-xo-space": "^0.16.0"
  },
  "engines": {
    "node": ">=6.0.0"
  },
  "eslintConfig": {
    "extends": "xo-space/esnext"
  },
  "files": [
    "generators/app"
  ]
}
